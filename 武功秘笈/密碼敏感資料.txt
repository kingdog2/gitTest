📁 內部專案使用：.env 模式
儲存： 密碼存在本地 .env。

Git： 只上傳 .env.example（範本），.env 本身加入 .gitignore（不進倉庫）。

打包： .exe 旁邊放一個設定檔，讓同事自行填寫 IP。

🌐 外部專案使用：API 模式
儲存： 密碼存放在遠端伺服器後端。

Git： 程式碼中只有 API 網址，沒有任何資料庫資訊。

打包： 使用者拿到 .exe 直接執行，安全性最高。